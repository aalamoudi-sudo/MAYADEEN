services:
  - type: web
    name: mayadeen-command-center
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: API_URL
        sync: false
